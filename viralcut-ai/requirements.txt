# Root pointer file - the real backend dependency list lives in backend/requirements.txt
# so the backend can be run from its own virtual environment.
#
#   cd backend
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# This file is kept in sync with backend/requirements.txt for convenience.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9
sqlalchemy>=2.0,<3.0
pydantic>=2.5,<3.0
faster-whisper>=1.0.0
opencv-python-headless>=4.9.0
numpy>=1.24,<2.0
